# DAA Console 项目审核报告

> 审核日期：2026-03-30
> 审核范围：功能完备性（第一性原理）+ UI/UX 交互逻辑

---

## 一、功能层面审核 — 从第一性原理出发

### 1.1 动态资产配置的本质是什么？

从第一性原理拆解，一个完整的动态资产配置系统需要回答 **五个核心问题**：

| # | 核心问题 | 对应能力 | DAA 现状 |
|---|---------|---------|---------|
| 1 | **买什么？** | 资产发现 + 筛选 + 研究 | ✅ 有（87 项精选资产目录 + 搜索 + 观察列表） |
| 2 | **买多少？** | 目标权重 + 仓位计算 | ✅ 有（目标权重设置 + 再平衡数学引擎） |
| 3 | **什么时候买/卖？** | 信号 + 时机判断 | ✅ 有（三维信号融合 + LLM 决策 + 市场体制识别） |
| 4 | **执行后怎么样？** | 执行 + 记录 + 归因 | ⚠️ 部分（模拟执行有，但归因分析薄弱） |
| 5 | **下次怎么做更好？** | 复盘 + 策略迭代 | ⚠️ 部分（决策结果追踪有，但反馈闭环不完整） |

### 1.2 已具备的核心能力（做得好的部分）

**资产发现层** — 覆盖面广，结构清晰

- 支持 10 种资产类别（股票、ETF、债券、商品、加密货币、外汇等）
- 覆盖 6 个市场区域（US / HK / CN / EU / JP / GLOBAL）
- 87 项精选资产目录，涵盖主要投资标的
- 统一的 `{MARKET}:{SYMBOL}` 资产键格式，多市场资产不混淆

**信号融合层** — 多维度决策，有冲突检测

- 四维信号：人类判断 35% + 技术面 25% + 新闻情绪 20% + 估值 20%
- 冲突检测机制：当信号方向矛盾时自动惩罚得分
- 宏观周期调节：根据市场阶段（滞胀/复苏/过热/通缩）微调资产偏好
- LLM 决策层作为仲裁者，可调整执行/缩减/跳过/增加优先级

**再平衡引擎** — 数学逻辑严谨

- 先卖后买（释放现金再部署），符合实际操作逻辑
- 支持日历触发（月度/季度/半年/年度）和偏移触发（阈值型）
- 有最小交易金额、最大单笔限制、冷却期等约束
- 隐式现金目标计算，避免过度投资

**市场数据层** — 多层缓存 + 优雅降级

- 三层缓存架构（快照 / 历史 / 原始响应）
- 状态分类（Fresh ≤15min / Stale ≤48h / Missing）
- 并发控制（6 路并发 + 2.6s 超时 + 重试）
- 多数据源（Yahoo Finance + 雪球 + 蛋卷 + Twitter RSS）

**风控层** — 执行前检查

- 风控门禁（pass / warn / block 三级）
- HHI 集中度指标
- 最大回撤计算
- block 状态直接阻止执行

### 1.3 功能缺失与薄弱环节

#### 🔴 P0 — 影响核心投资决策

**1. 缺少组合级别的绩效归因（Performance Attribution）**

当前系统能记录交易、计算 P&L，但无法回答"收益/亏损是哪些因素贡献的？"。一个完整的归因系统应包含：
- **资产归因**：每个持仓对组合收益的贡献度
- **决策归因**：再平衡决策 vs 不动（择时效果）
- **因子归因**：市场 beta、行业、地区、风格（价值/成长）各贡献多少
- **基准对比**：组合 vs 60/40 基准 vs 等权 vs 标普 500

当前 `src/core/backtest/` 有 Attribution 相关代码框架，但未在前端完整呈现。Strategy Lab 的回测是独立的策略对比，不是实际持仓的归因。

**2. 缺少组合整体风险度量仪表盘**

当前有单资产级别的 HHI 集中度和最大回撤，但缺少：
- **组合波动率**（Portfolio Volatility）
- **VaR / CVaR**（在险价值 / 条件在险价值）
- **相关性热力图**（资产间相关性矩阵）— 代码中 `correlationService.ts` 存在但前端未展示
- **压力测试**（如果 2008 / 2020 / 2022 重演，组合会怎样？）
- **Sharpe / Sortino / Calmar Ratio** 等风险调整收益指标

**3. 现金管理策略不够精细**

当前现金分为三类（运营储备、战略现金、可投闲置），但：
- 没有现金拖累（Cash Drag）的量化提醒
- 缺少自动现金扫入（Cash Sweep）的执行建议
- LLM 给出的 cashAdvice（hold / deploy / await）没有量化落地

#### 🟡 P1 — 影响用户持续使用

**4. 税务损失收割（Tax Loss Harvesting）有代码但未完整接入**

`taxLossHarvestingService.ts` 存在，但：
- 前端无对应入口
- 未与再平衡提案联动（应在生成提案时自动标记 TLH 机会）
- 缺少 Wash Sale Rule 检查

**5. 股息追踪已有但展示不足**

`dividend/` 模块存在，cron 任务也在跑，但：
- 持仓页面未展示预期股息收益率
- 没有股息日历（什么时候分红、分多少）
- 缺少再投资 vs 提现的策略选项

**6. 多币种组合的汇率风险暴露缺失**

系统支持多币种资产（USD / HKD / CNY），FX 汇率也在刷新，但：
- 没有汇率风险暴露的可视化（按币种的配置饼图）
- 缺少未对冲 vs 已对冲的风险量化
- 没有汇率变动对组合 P&L 的影响归因

**7. 基金经理持仓追踪（HF Holdings）已有 cron 但前端薄弱**

`app/api/daa/hf/` 目录存在，每日 8am UTC 跑，但：
- 前端未展示"大佬在买什么"的信息
- 未与资产发现/信号融合联动

#### 🟢 P2 — 锦上添花

**8. 缺少"如果…会怎样"（What-If）模拟器**

用户无法在执行前模拟："如果我把 AAPL 从 10% 调到 15%，组合会怎样？"

**9. 缺少通知的智能降级**

通知支持 Email / Telegram / 飞书，但：
- 没有通知频率控制（用户可能被轰炸）
- 缺少紧急程度分级（drift 超 2% vs 超 10% 应该不同通知级别）

**10. 缺少资产间替代品建议**

当某资产信号为 reduce_or_avoid 时，应建议同类替代品（如 AAPL 减仓 → 考虑 MSFT 或 QQQ）

---

## 二、UI/UX 交互层面审核

### 2.1 整体页面架构

当前 6 页导航结构：

```
投委会 (Today) → 持仓 (Portfolio) → 调仓 (Rebalance) → 交易记录 (Trades) → 策略实验室 (Strategy Lab) → 设置 (Settings)
```

### 2.2 发现的交互问题

#### 🔴 Critical — 流程断裂

**问题 1：Today 页"采纳"决策后无出口**

用户在投委会页面看到 AI 建议，点击"采纳"后：
- 决策被记录，UI 显示"已采纳"
- **但没有"前往调仓"的按钮或引导**
- 用户必须手动点击侧边栏导航到调仓页
- 调仓页加载时，也不会自动关联 Today 页已采纳的决策

**实际流程 vs 期望流程**：
```
❌ 当前：Today 采纳 → 死胡同 → 手动导航 → 调仓页（无上下文）
✅ 期望：Today 采纳 → "前往调仓 →" 按钮 → 调仓页（已预选相关资产）
```

**问题 2：Trades 页面空状态链接指向已废弃路径**

Trades 页面的 3 处空状态 CTA 仍指向 `/daa/dashboard/workbench?tab=rebalance`，这是旧路径。虽然 `/workbench` 有重定向逻辑兜底，但：
- 多了一次不必要的跳转
- URL 在浏览器地址栏中闪烁变化，体验不专业
- 涉及文件：`TradesSections.tsx` 第 140、144、148 行

**问题 3：调仓执行完成后无引导**

用户完成再平衡执行后：
- 通知栏显示执行回执（成功/部分/失败）
- **但没有"查看交易记录"或"返回投委会"的链接**
- 用户停留在调仓页面，不知道下一步该做什么

#### 🟡 Moderate — 体验割裂

**问题 4：Today 页与 Rebalance 页的 ActionWorkflow 组件重复**

两个页面都渲染了同一个 `ActionWorkflow`（5 步工作流组件）。虽然底层通过 `useWorkbenchPageModel` 共享状态，但：
- 用户在 Today 页开始了工作流的第 2 步，切到 Rebalance 页后，同步状态可能导致困惑
- 两个入口的心智模型不同：Today = "今天要不要操作？"，Rebalance = "具体怎么调仓"
- 建议：Today 页只展示结论和快捷入口，完整 5 步流程只在 Rebalance 页

**问题 5：持仓页的手动下单与调仓流程完全隔离**

Portfolio 页可以通过 `MarketOrderDialog` 手动买卖，但：
- 手动订单不会出现在调仓提案中
- 手动订单不触发风控检查
- 交易记录页的"再平衡周期"tab 里看不到手动单
- 用户可能困惑：同一个系统，两套下单逻辑，数据不互通

**问题 6：Settings 修改后缺少即时生效的反馈**

Settings 保存后，tooltip 提示"请重新生成或刷新建议，使新配置应用到当前再平衡周期"，但：
- 没有直接跳转到调仓页的快捷操作
- 用户可能保存后忘记刷新，导致旧配置继续运行
- 建议：保存成功后提供"立即刷新调仓建议"的 action button

**问题 7：Proposal 详情需要逐条展开，无批量查看**

RebalanceProposalList 中每条提案的决策上下文（信号得分、LLM 置信度、市场体制等）默认折叠：
- 理由截断为 50 字符
- 需要逐条点击展开
- 没有"展开全部"的切换按钮
- 对需要全面审阅的用户来说，交互成本很高

**问题 8：Portfolio 页缺少出站导航**

持仓页是一个"信息孤岛"：
- 查看持仓后想调仓？需要手动点侧边栏
- 发现某个持仓偏移过大？没有"立即再平衡"的快捷入口
- 管理观察列表后想看信号分析？没有跳转到 Today 页的入口

#### 🟢 Minor — 细节优化

**问题 9：空组合引导流程不完整**

`EmptyPortfolioGuide` 展示了 3 个步骤（添加资产 → 设定权重 → 生成建议），但：
- 没有明确的"设置完成后去调仓"的 CTA
- 第 3 步结束后用户需要自行寻找下一步

**问题 10：Dialog-first 设计缺少操作确认反馈**

所有突变操作（下单、校准、删除观察列表）都通过 Modal Dialog 完成：
- 关闭 Dialog 后回到表格，没有 Toast 确认操作结果
- 没有高亮刚修改的行
- 用户可能不确定操作是否成功

**问题 11：侧边栏折叠状态下没有 Badge 提示**

侧边栏折叠后只显示图标，但：
- 调仓页有待处理的提案时，侧边栏没有数字 Badge
- Today 页有新的 AI 建议时，侧边栏没有红点提示
- 用户在其他页面时，不知道是否有待处理事项

---

## 三、改进优先级建议

### 立即修复（1-2 天）

1. **修复 Trades 页废弃链接** → 改为 `/daa/dashboard/rebalance`
2. **ActionCard 采纳后加"前往调仓"按钮**
3. **执行完成后加"查看交易记录"链接**

### 短期优化（1-2 周）

4. **Today 页与 Rebalance 页职责分离** — Today 只展示摘要 + 快捷入口
5. **Portfolio 页增加出站导航**（"去调仓"、"去信号分析"）
6. **Settings 保存后增加"立即刷新调仓建议"action**
7. **Proposal 列表增加"展开全部"按钮**
8. **操作后增加 Toast 反馈**

### 中期建设（1-2 月）

9. **组合级绩效归因模块** — 资产归因 + 决策归因 + 基准对比
10. **组合风险仪表盘** — 波动率、VaR、相关性热力图、Sharpe Ratio
11. **相关性矩阵前端展示** — 代码已有，需接入 UI
12. **税务损失收割接入再平衡提案**
13. **What-If 模拟器**

### 长期规划（3+ 月）

14. **汇率风险暴露可视化 + 对冲建议**
15. **股息日历 + 再投资策略**
16. **基金经理持仓追踪前端**
17. **通知智能降级 + 紧急程度分级**
18. **资产替代品推荐引擎**

---

## 四、总结

DAA Console 在**资产发现 → 信号融合 → 再平衡计算 → 模拟执行**的核心链路上已经相当完整，信号融合引擎的四维设计加上冲突检测和宏观调节，在同类个人工具中算得上精巧。

最大的短板在两个方面：

**功能层面**：缺少"执行后"的闭环 — 绩效归因、组合级风险度量、决策复盘的数据支撑。投资是一个持续迭代的过程，当前系统在"做决策"上投入很多，在"评估决策质量"上还不够。

**交互层面**：页面之间的导航是割裂的。核心流程（Today 决策 → 调仓执行 → 查看记录）在每个环节结束时都会遇到"死胡同"，需要用户自行通过侧边栏跳转。对于一个工作流驱动的金融工具来说，这种断裂会显著增加操作摩擦。

好消息是，大部分交互问题都是"加几个按钮和链接"级别的修复，代码架构上并没有阻碍。功能层面的补全则需要更系统的规划，建议优先从绩效归因和组合风险仪表盘入手，因为它们直接影响用户的投资决策质量。
