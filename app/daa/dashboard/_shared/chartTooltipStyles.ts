import type { CSSProperties } from "react";

export const daaChartTooltipContentStyle: CSSProperties = {
  backgroundColor: "var(--card)",
  border: "1px solid var(--border)",
  borderRadius: "var(--radius-md)",
  boxShadow: "none",
  color: "var(--text)",
};

export const daaChartTooltipItemStyle: CSSProperties = {
  color: "var(--text)",
};

export const daaChartTooltipLabelStyle: CSSProperties = {
  color: "var(--muted)",
  fontWeight: 600,
};
